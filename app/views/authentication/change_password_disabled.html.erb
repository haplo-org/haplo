<%
  @page_title = T(:Authentication_Title_Change_password)
  @breadcrumbs = [:account]

  if @change_msg == nil || @change_msg.length == 0
    @change_msg = T(:Authentication_Please_contact_your_administrator_for___)
  end
-%>

<p><%= h(@change_msg) %></p>
