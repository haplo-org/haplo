<%
  @page_title = 'Create new Intranet Page from Tray Contents'
  @breadcrumbs = [:tray, ['/do/tray', 'Tray']]
%>

<%= render(:partial => 'shared/editor_for_new_object', :data_for_template => {:object => @page}) %>
