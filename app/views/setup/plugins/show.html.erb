<%
  @page_title = h(@plugin_factory.plugin_name)
-%>

<h1><%= @page_title %></h1>

<p><%= h(@plugin_factory.plugin_description).gsub("\n","</p><p>") %></p>

<div class="z__management_extra_buttons_holder">
  <form method="GET" action="/do/setup/plugins/uninstall/<%= @plugin_factory.name %>">
    <input type="submit" value="Uninstall">
  </form>
</div>

<%= sys_mng_update_submenu(@page_title, "/do/setup/plugins/show/#{@plugin_factory.name}") %>
