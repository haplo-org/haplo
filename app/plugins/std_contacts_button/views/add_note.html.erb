<%
  @page_title = 'Add note to '+h(@add_to.first_attr(KConstants::A_TITLE).to_s)
  @breadcrumbs = [[object_urlpath(@add_to), 'Cancel']]
%>

<%= render(:partial => 'shared/editor_for_new_object', :data_for_template => {:object => @object}) %>
