each(pages) {
  <button
    name="__goto"
    value=form.specification.formId
    class=[if(complete){"z__docstore_nav_complete"}else{"z__docstore_nav_todo"} if(active){"z__docstore_nav_selected"}]
  >
    form.specification.formTitle
  </button>
}
