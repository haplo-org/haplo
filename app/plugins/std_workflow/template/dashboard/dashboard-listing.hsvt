pageTitle(
    dashboard._displayableTitle ": " stateName
    if(tagDisplayableName) {
        " (" tagDisplayableName ")"
    }
)
backLink(spec.path ? *dashboard._linkParameters) { "Dashboard" }

each(list) {
  <p> <a href=url> title </a> </p>
}
