pageTitle(
    dashboard._displayableTitle ": " stateName
    if(tagDisplayableName) {
        " (" tagDisplayableName ")"
    }
)
backLink(dashboard.spec.path ? *dashboard._linkParameters) { "Dashboard" }

each(list) {
  <p> <a href=url> title </a> </p>
}
